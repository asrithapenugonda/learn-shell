# learn-shell
- A shell is a program that acts as an interpreter. It accepts commands from the user, interprets them, and executes them

- Shell scripting is writing commands and scripting logic in a file so that tasks can be executed repeatedly or automatically
- Bash is a shell and scripting language commonly used on Linux systems